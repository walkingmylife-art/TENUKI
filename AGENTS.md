AGENTS.md

PRINCIPLES.md と FORBID.md を読むこと。

応答は日本語。

現在のユーザー指示と現在のソースコードを最優先する。

過去の計画、コメント、記憶、既存テスト、過去の成功例は、現在の作業にそのまま持ち込まない。

過去事例を使う場合は、個別の関数名・事故名・処理名ではなく、目的・責務・境界・観測点へ圧縮して使う。

作業の基本

作業AIに無謬性を要求しない。

作業AIの責務は、指定された範囲を実装し、変更点を見える形で返すこと。

設計判断、違和感検出、削ぎ落とし、責務境界の確認は、このチャット側で行う。

MDは事故防止集ではない。

MDは、あとから変更差分を確認できる足場として使う。

実装時に見ること

実装時は、まず以下を見る。

今回の目的
変更範囲
触らない範囲
入力
出力
読むもの
書くもの
責任境界
最小の完了条件

authority validator を使った場合は、それを通したことを正しさの根拠にしない。

見るべきことは、その validator が今回の責任境界に合っているかである。

禁止の扱い

禁止を増やして判断をマスクしない。

禁止は、破ると責任境界が壊れるものだけに絞る。

それ以外は、禁止ではなく観測対象として扱う。

報告

小変更では、重い報告テンプレを要求しない。

最低限、以下を返す。

変更したファイル
変更した主な箇所
削除した旧経路があればそれ
残した経路があればそれ
実行した確認
未確認リスク

大きい変更や責務移動がある場合だけ、before / after や追加の観測表を使う。